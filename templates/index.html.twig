<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Home | Dijiti{% endblock %}</title>
    {# Fonts #}
    <link rel="dns-prefetch" href="//fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css?family=Nunito" rel="stylesheet">
    {# Bootstrap #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
</head>
<body>

    {# Inizio header #}
    <header>
        <nav class="navbar navbar-expand-md navbar-dark bg-dark shadow-sm">
        <div class="container">
            <img class="navbar-brand" src="https://dijiti.com/img/logo-default-slim-dark.png?v=1" alt="dijiti-logo">

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <!-- Left Side Of Navbar -->
                <ul class="navbar-nav mr-auto">
                    <a class="nav-link" href="{{ path('index_index') }}">Home</a>
                    <a class="nav-link" href="{{ path('index_gestione_utenti') }}">Gestione utenti</a>
                    <a class="nav-link" href="{{ path('index_gestione_database') }}">Gestione database</a>
                </ul>                
            </div>
        </div>
        </nav>
    </header>
    {# Fine header #}

    {# Inizio main #}
    <main>
        <div class="flex-center position-ref full-height">
            <div class="content">
            {% block main %}
                <div class="title">
                    Benvenuto nella prova tecnica di Michael Alesiano
                </div>
            {% endblock %}    
            </div>
            <div class="myTable">
            {% block table %}
            {% endblock %}
            </div>
        </div>
    </main>
    {# Fine main #}
    
</body>
</html>

{# Styles #}
    <style>
        html, body {
            color: white;
            font-family: 'Nunito', sans-serif;
            font-weight: 200;
            margin: 0;
        }
        
        {# Header #}
        {# Navbar #}
        .navbar {
            width: 100%;
            background-color: black;
            position: fixed;
            z-index: 1;
        }

        .navbar a {
            margin-left: 14px;
            color: white;
        }

        .navbar .navbar-brand {
            width: 100px;
            height: auto;
            margin-right: 20px;
        }
        
        {# Main #}
        .full-height {
            height: 100vh;
            background: linear-gradient(#eb6209, #000000);
        }

        .flex-center {
            align-items: center;
            display: flex;
            justify-content: center;
        }

        .position-ref {
            position: relative;
        }

        .title {
            font-size: 70px;
        }

        {# Gestione utenti #}
        .invia {
            margin-top: 10px;
            border-radius: 5px;
        }

        .alert {
            margin-top: 10px;
        }

        {# Table #}
        .myTable {
            margin-left: 180px;
        }

        table th {
            padding-right: 100px;
            border: 1px solid black;
            font-size: 20px;
        }

        table td {
            border: 1px solid black;
        }

    </style>